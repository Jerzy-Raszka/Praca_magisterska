import 'package:flutter/material.dart';

class DishDialog extends StatefulWidget {
  const DishDialog({super.key});

  @override
  State<DishDialog> createState() => _DishDialog();
}

class _DishDialog extends State<DishDialog> {
  @override
  void initState() {
    super.initState();
  }

  //TODO: change Text into overlay widget with dish data and button for subscribing or deleting the dish

  @override
  Widget build(BuildContext context) {
    return Text('test');
  }
}
